# Add videos here

Place your MP4 or WebM video clips in this folder. Then update the matching `src` inside the `VIDEO MOMENTS` section in `../../index.html`.

Examples already prepared in the page:

- `badminton-rally.mp4`
- `keyboard-session.mp4`
